# Testing wasm & typescript

## About

Testing wasm in a typescript project

## 🚀 Usage

Compile wasm code with `wasm-pack build` in wasm folder

Then use `yarn install` and `yarn start` in www folder
